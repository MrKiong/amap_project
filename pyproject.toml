[project]
name = "amap-project"
version = "0.1.0"
description = "Personal food recommendation Agent CLI MVP"
requires-python = ">=3.11"
dependencies = [
    "openai>=2.0.0",
]

[tool.uv]
package = false
